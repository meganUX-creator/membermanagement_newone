# 产品需求文档 (PRD) - 查看会员详情模块

## 1. 模块概述
本模块定义了后台管理系统中「查看详情」抽屉（Drawer）的功能规格与介面配置。主要提供管理员以唯读（Read-only）的方式快速检视特定会员的基础帐户资讯、联络方式、审核纪录、提现绑定资料，以及设备与网路 IP 轨迹。

## 2. 抽屉全局配置
* **标题与状态显示：**
  * 显示操作对象（例：`查看详情 · mingv0717001`）。
  * 显示会员身分标签（例：`代理会员`、`VIP会员`）。
  * 显示目前帐户状态并附带颜色标示（例：`目前状态：冻结`）。
* **页签结构 (Tabs)：** 提供分类检视，包含以下页签：
  1. **会员信息**：检视会员基础资料与联络方式（预设展开）。
  2. **会员审核**：检视会员相关的审核纪录。
  3. **提现信息**：检视会员绑定的出款帐户与提现纪录。
  4. **设备与IP纪录**：检视会员登入的设备与网路 IP 轨迹。

---

## 3. 分页规格：会员信息 (Member Information)
本页签内的所有栏位皆为唯读展示，部分敏感资讯（如暱称）视权限或隐私规范进行脱敏（Masking）处理。

### 3.1 基本资料 (Basic Info)
| 栏位名称 | 类型 | 说明 / 范例 |
| --- | --- | --- |
| 头像 | 图片/图标 | 显示会员的头像设定 |
| 用户暱称 | 唯读文字 | 显示会员暱称，可支援部分脱敏（例：`mi***01`） |
| 帐号 | 唯读文字 | 显示系统登入帐号（例：`mingv0717001`） |
| 真实姓名 | 唯读文字 | 显示会员填写的真实姓名，若无则显示 `-` |
| 出生年月日 | 唯读文字 | 显示格式为 YYYY-MM-DD（例：`1991-02-11`） |

### 3.2 联络方式 (Contact Info)
| 栏位名称 | 类型 | 说明 / 范例 |
| --- | --- | --- |
| 电话 | 唯读文字 | 显示绑定之手机号码，若无则显示 `-` |
| Email | 唯读文字 | 显示绑定之电子信箱（例：`mingv0717001@example.com`） |
| QQ | 唯读文字 | 显示绑定之 QQ 帐号 |
| 微信 (WeChat) | 唯读文字 | 显示绑定之微信帐号 |
| Zalo | 唯读文字 | 显示绑定之 Zalo 帐号/号码 |
| WhatsApp | 唯读文字 | 显示绑定之 WhatsApp 号码 |
| Telegram | 唯读文字 | 显示绑定之 Telegram 帐号（例：`@mingv0717001_tg`） |
| Facebook | 唯读文字 | 显示绑定之 Facebook 连结（例：`fb.me/mingv0717001`） |

### 3.3 备注 (Remarks)
| 栏位名称 | 类型 | 说明 / 范例 |
| --- | --- | --- |
| 备注 | 唯读文字 | 显示管理员对该会员添加的一般备注内容，若无则显示 `-` |
| 回访备注 | 唯读文字 | 显示客服或管理员的回访纪录与状态（例：`暂无回访备注`） |

---

## 4. 分页规格：会员审核 (Member Audit)
此分页主要用于展示会员各项基本资料与联络方式的变更申请纪录，提供管理员比对修改前后的差异，并针对该笔申请进行审核操作。

### 4.1 审核列表 (Data Table)
| 栏位名称 | 类型 | 说明 / 范例 |
| --- | --- | --- |
| 变更项目 | 纯文字 | 显示会员申请变更的资料类型（例：`真实姓名`、`电话` 等） |
| 修改前 | 纯文字 | 显示变更前的原始资料，若原先无资料则显示 `未填写` |
| 修改后 | 纯文字 | 显示会员新提交的资料内容（例：`1991-02-11`） |
| 申请时间 | 时间戳 | 显示该笔变更提交的精确时间（格式：`YYYY-MM-DD HH:mm:ss`） |
| 操作 | 按钮组 | 提供针对该笔纪录的审核动作 |

### 4.2 操作按钮定义
* **通过**：核准该笔资料变更，系统将覆盖并更新为「修改后」的资料。
* **拒绝**：驳回该笔资料变更，维持「修改前」的状态（通常会要求填写拒绝原因）。
* **重置**：撤销当前的审核状态或清空该栏位的异常状态，重新开放审核或重置为未审核状态。

---

## 5. 分页规格：提现信息 (Withdrawal Information)
此分页用于唯读展示会员当前绑定的所有出款帐户资料，包含实体银行卡、虚拟货币钱包及第三方支付。列表不提供新增、编辑或删除功能。

### 5.1 提现帐户列表 (Data Table)
| 栏位名称 | 类型 | 说明 / 范例 |
| --- | --- | --- |
| 出款类型 | 纯文字 | 显示会员绑定的提现渠道类型（例：`银行卡`、`虚拟货币`、`支付宝`）。 |
| 卡号/钱包/支付宝帐号 | 纯文字 | 显示具体的收款帐号，支援中间字元隐藏脱敏处理（例：`622202******1234`）。 |
| 银行/币种 | 纯文字 | 显示对应的金融机构名称或虚拟币协议与币种（例：`中国工商银行 / CNY`、`USDT (TRC20)`）。 |
| 银行地址/备注 | 纯文字 | 显示实体银行的开户分行资讯，或第三方支付的真实姓名/备注，若无资料则显示 `-`。 |
| 状态 | 状态标签 | 显示该提现方式目前的可用状态，并以颜色区分（例：绿色 `启用`、红色/灰色 `停用`）。 |

---

## 6. 分页规格：设备与IP纪录 (Device & IP Records)
此分页展示会员的「最新登入」与「首次注册」时的网路及硬体轨迹，提供安全与风控人员快速辨识潜在异常行为。页面分为「最新登入纪录」与「注册纪录」两个主要区块，栏位结构一致。

### 6.1 纪录区块栏位定义
| 栏位名称 | 类型 | 说明 / 范例 |
| --- | --- | --- |
| IP 地址 | 文字 + 按钮 | 显示 IP 数值并解析对应的地理位置。附带「一键复制」图示。<br>*(例：`54.150.111.152 (Japan, Tokyo)`)* |
| 时间 | 时间戳 | 显示触发该纪录的精确时间。<br>*(例：`2026-07-28 16:30:42`，若无资料则显示 `-`)* |
| 设备号 | 文字 + 按钮 | 显示装置唯一识别码 (Device ID)，附带「一键复制」图示。<br>*(例：`ee5868d85af7f68cf0...`)* |
| 同IP人数 | 数值超连结 | 显示系统中与此 IP 相同的关联会员总数。点击后触发第三层展开。<br>*(例：`4,703`)* |
| 同设备人数 | 数值超连结 | 显示系统中与此设备号相同的关联会员总数。点击后触发第三层展开。<br>*(例：`28`)* |

---

## 7. 交互规格：详细内容第三层展开 (Nested Drill-down)

### 7.1 触发情境与展现形式
* **触发条件**：在「设备与IP纪录」中点击 「同IP人数」 或 「同设备人数」 的数值超连结。
* **UI 呈现**：采用第三层抽屉（Nested Drawer）叠加于当前抽屉之上，或采用弹窗（Modal / Dialog）。
* **标题**：依据点击类型动态带入（例：`同 IP 关联会员列表 (54.150.111.152)`）。

### 7.2 第三层列表内容 (Data Table)
为唯读展示，协助核对关联帐号的状态：
| 栏位名称 | 说明 |
| --- | --- |
| 会员帐号 | 显示关联的会员帐号名称，支援点击直接切换检视该会员详情。 |
| 帐户状态 | 显示该关联帐号当前状态（正常 / 冻结 / 停用）。 |
| VIP 等级 | 显示该关联帐号的 VIP 层级。 |
| 注册时间 | 显示该关联帐号的建立时间，用于判断是否为恶意批次注册。 |
| 最后登入时间 | 显示该关联帐号最后活跃时间。 |

### 7.3 操作与限制
* **导航操作**：提供 `关闭` 或 `返回上一层` 按钮，无缝退回至原本的「查看详情」抽屉。
* **权限限制**：第三层展开仅供资料检视，不提供直接修改关联会员资料的编辑功能。