# PRD: 自訂欄位抽屜 - 分組看板優化 (Antigravity IDE 執行指引)

## 1. 任務目標 (Objective)
**目標檔案:** `content.html`
**目標組件:** 由主表格視圖中的「自訂欄位」按鈕所觸發的底部抽屜/側邊面板。
**任務:** 將現有的自訂欄位設定介面，重構為「3:2:1 比例的分組看板 (Grouped Kanban)」佈局。目標是解決使用者認知超載問題，並支援透過「按鈕」與「拖曳」進行精準的跨層級欄位轉移。

## 2. UI/UX 架構規格 (Architecture & Layout)
在 `content.html` 中實作一個 Flexbox 容器，嚴格依循 `3:2:1` 寬度比例 (flex-grow ratio) 劃分三欄：

*   **第一欄: 主表格欄位 (`flex-[3]`)**
    *   **UI:** 佔據最大寬度 (`w-full`)。內部為單欄垂直列表（若空間極大可改為雙欄網格）。
    *   **行為:** 顯示已啟用的頂層欄位。系統鎖定欄位 (`isLocked: true`) 必須固定在此區，禁止移出。
*   **第二欄: 展開卡片欄位 (`flex-[2]`)**
    *   **UI:** 必須包含「手風琴式 (Accordion) 分類群組」（例如：基本資料、資金與存款）。
    *   **行為:** 欄位必須歸屬於特定的分類標題下。分類標題本身需具備「隱藏/顯示」切換功能。
    *   **沉底機制:** 當整個分類被設為隱藏時，該群組區塊需自動移至第二欄的最下方（沉底），並變為灰階半透明，內部列表自動折疊。
*   **第三欄: 備用欄位庫 (`flex-[1]`)**
    *   **UI:** 寬度最窄。內部欄位建議使用「藥丸標籤 (Pill list / flex-wrap)」緊湊排列。
    *   **行為:** 存放所有未啟用的欄位。頂部應保留空間放置搜尋框 (Search Bar)。

## 3. 核心互動邏輯 (Interaction Logic)
系統必須同時支援以下兩種操作模式，確保操作確定性 (Deterministic Actions)：

1.  **按鈕驅動 (Button-Driven - 優先實作):**
    *   每個欄位均需顯示操作按鈕。
    *   **同區塊排序:** 提供 `[上移]`、`[下移]` 按鈕。邊界值需禁用該按鈕 (Disabled state)。
    *   **跨區轉移:**
        *   第一欄提供 `[移至卡片]`、`[隱藏(X)]`。
        *   第二欄提供 `[提至表格]`、`[隱藏(X)]`。
        *   第三欄提供 `[+加至表格]`、`[+加至卡片]`。
    *   **智慧路由:** 當透過按鈕將欄位移入「卡片區」時，系統需讀取該欄位的 `category` 屬性，並將其自動放入正確的分類群組中。
2.  **拖曳支援 (Drag & Drop):**
    *   支援欄位的跨欄拖放，以及第二欄內分類群組的上下拖放。
    *   **視覺回饋:** 拖曳時，目標放置區必須顯示清晰的指示線 (Drop Indicator Line)。

## 4. Antigravity IDE 執行步驟 (Execution Steps for Agent)
1.  **定位 DOM:** 掃描 `content.html`，定位觸發自訂欄位的按鈕以及對應的 Drawer 容器節點。
2.  **狀態重構 (State Management):** 確保負責該抽屜的狀態 (State) 能夠處理巢狀結構 (Columns -> Categories -> Fields) 以及分類群組的 `isHidden` 狀態。
3.  **實作 3:2:1 佈局:** 清除舊版單一列表，使用 Tailwind CSS (`flex`, `flex-3`, `flex-2`, `flex-1`) 建立三欄佈局。
4.  **實作事件綁定:** 
    *   綁定按鈕點擊事件，處理陣列元素的 Splice/Push，並觸發畫面重新渲染。
    *   實作拖放 (DragStart, DragOver, Drop) 事件處理常式。
5.  **樣式微調 (即時預覽):** 移除抽屜背景的模糊效果 (`backdrop-blur`)，改用輕微的暗色遮罩 (如 `bg-slate-900/10`)，確保使用者在操作抽屜時，能清楚看見底層表格的即時變動。