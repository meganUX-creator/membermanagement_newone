🎯 AI 前端开发需求：企业级双层资料关联式「自订栏位抽屉面板」

请为我实作一个用于复杂表格管理的「自订栏位」组件。此组件需处理「主表格原生栏位」与「详细卡片额外资料」之间的双向连动与显示设定。请严格遵循以下 UX/UI 逻辑与架构进行开发。

1. 介面架构与版面行为 (Layout & Behavior)

触发机制：在列表模式的页面中，点击「自订栏位」按钮后，开启底部抽屉 (Bottom Drawer)。

视觉呈现：

抽屉必须采用 绝对定位 (Absolute/Overlay) 覆盖于表格上方，并带有向上扩散的阴影，绝不可挤压或推挤原有表格的高度。

提供两段式高度切换：顶部需有「展开/缩小」按钮，让抽屉在 50vh 与 85vh 之间平滑切换。

内部布局 (无页签设计)：

抽屉内部采「主副驾驶舱」左右双栏平铺设计 (Side-by-Side)。

左侧 (第一层级)：「主表格」的栏位设定区（控制标题与顺序）。

右侧 (第二层级)：「展开卡片」的资料源区（提供额外详细栏位）。

2. 右侧面板：资料源 (第二层级) 核心逻辑

此区为资料仓库，分为「多栏位卡片」与「单行独立区块」。

自适应网格 (Auto-fit Grid)：多栏位卡片内的子项目排版，必须使用 CSS Grid (auto-fit + minmax)，使其依照宽度自动在同行显示 2 到 4 个栏位，由左至右流动排列。

【跨层级提升 Promote】(核心互动)：

每个子栏位旁预设提供 + (加入) 按钮。点击后，该栏位立即被「提升」到左侧主表格清单中。

栏位被提升后，+ 按钮隐藏，原地替换为 🔒 (锁头) 按钮。允许使用者直接在右侧点击锁头，将该栏位设定为左侧表格的「冻结状态」。

3. 左侧面板：主表格设定 (第一层级) 核心逻辑

此区控制表格的实际渲染，分为「固定在左侧(冻结区)」与「可卷动区块」两个视觉群组。

【双轨排序机制】(重点防呆)：

拖曳 (Drag & Drop)：项目左侧提供握把图示，支援跨区块与同区块内的拖曳排序。拖越边界时需自动转换其冻结属性。

点击微调：项目右侧提供「上移/下移」箭头按钮。按钮操作严格限制在所属区块内，不可跨越冻结/卷动边界。达边界时按钮需呈现 disabled 状态。

【冻结切换 Lock/Unlock】：点击锁头图示，项目能在「冻结区」与「卷动区」间自动跳跃移动。

【双向退回机制 Demote】(UX防死胡同)：

原生表格栏位：仅提供 Checkbox 控制显示/隐藏。

从右侧提升过来的「卡片栏位」：必须额外提供红色「垃圾桶」按钮。点击后，彻底从左侧移除，并在右侧面板恢复为 + (可加入) 状态。

4. 表格端的视觉与连动要求 (Table View Integration)

无延迟同步：抽屉内的任何勾选、排序、冻结操作，必须透过 React State 即时反映在底层表格上。

防挤压布局 (Horizontal Scroll Protection)：外层容器需 overflow-x-auto，表格本身需 min-w-max。无论栏位多寡，绝对不换行。

完美冻结十字交叉点 (Sticky CSS)：

表头往下卷动时必须吸附顶部 (sticky top-0 z-30)。

冻结区栏位向右滑动时必须吸附左侧 (sticky left-* z-10/40)。

需利用回圈动态计算多个冻结栏位并排时的 left 偏移量 (offset)。

客制化渲染：特殊栏位（如头像、彩色标签）提升到主表格时，需保留 UI 组件渲染（如圆形头像、Badge），而非显示纯文字或 Object。